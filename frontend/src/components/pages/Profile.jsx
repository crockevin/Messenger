import ProfileContent from '../ProfileContent' // Contains bottom nav and body (conversations, etc)
import ProfileHeader from '../ProfileHeader' // Top Nav bar

export default function Profile() {
  return (
    <>
      <ProfileHeader />
      <ProfileContent />
    </>
  )
}
